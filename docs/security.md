# Безопасность

Credential никогда не экспортируется в workflow JSON. Пароли, SMTP/Gmail OAuth, API keys и webhook secrets хранятся только в n8n credentials/runtime. В публичный репозиторий включаются schema, templates, scrubbed workflow exports и synthetic examples.
